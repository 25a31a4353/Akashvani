import type { IndiaLocationContext } from "@shared/india";

export function SelectedLocationInfrastructure({ context }: { context: IndiaLocationContext }) {
  const sample = context.infrastructure.items.slice(0, 6);
  return <section data-testid="selected-location-infrastructure" className="mt-3 rounded-2xl border border-[#cfe3e7] bg-white p-3.5 shadow-[0_10px_24px_rgba(28,55,70,.04)]"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#1d788d]">Nearby mapped infrastructure</p><p className="mt-1 text-xs font-bold text-[#294d60]">{context.infrastructure.status === "LIVE OSM FACILITY SAMPLE" ? `${context.infrastructure.items.length} facility records available` : "Facility sample unavailable"}</p></div><span className="rounded-full bg-[#edf7f8] px-2 py-1 text-[9px] font-bold text-[#1d7084]">{context.infrastructure.status}</span></div>{sample.length ? <div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{sample.map(item => <div key={item.id} className="rounded-lg bg-[#f4f9fa] p-2"><p className="truncate text-[11px] font-semibold text-[#315669]">{item.name}</p><p className="mt-0.5 text-[9px] text-[#718892]">{item.type}</p></div>)}</div> : <p className="mt-2 text-[10px] leading-relaxed text-[#718892]">No nearby facility records were returned; this is not evidence that facilities are absent.</p>}<p className="mt-3 text-[9px] leading-relaxed text-[#718892]">{context.infrastructure.source}{context.infrastructure.observedAt ? ` Updated ${new Date(context.infrastructure.observedAt).toLocaleString("en-IN")}.` : ""}</p></section>;
}
