# Map redesign verification notes

The command-view shell now matches the supplied composition: a narrow dark region/hazard sidebar, a central map canvas with floating search and Layers/Legend controls, and a dark AI relocation recommendation panel.

The satellite endpoint fills the selected area, but the preview currently shows an obvious seam between the imagery tiles and the darkened OpenStreetMap fallback. This needs to be resolved before delivery by choosing one visually continuous basemap treatment.

The selected assessment area is now framed locally rather than at the previous nationwide zoom. The recommendation panel correctly uses existing assessment data for the selected area and candidate site, and the route/report actions remain connected to existing callbacks.


The blended basemap now renders continuously across the command canvas while preserving visible satellite texture and map labels. The Wayanad marker is visible at the selected center, and the desktop three-column balance is close to the supplied reference. The mobile preview stacks the map above the recommendation panel and keeps search, map controls, attribution, and the map-context badge visible.


Final visual check: the default route opens directly into the dark command map, while `?workspace=dashboard` still renders the previous analytics workspace with the shared map component and contextual panel. No new browser console errors were observed in the latest preview logs.


The final command-view screenshot shows the dashed planning corridor from the selected Wayanad marker to the nearest mapped context point, with the sidebar and map footer explicitly stating that candidate-site coordinates are not yet verified. The route remains a visual planning aid until verified destination geometry is added to the data model.
Location consistency findings (2026-08-26): The Assam deep link resolves to `india-assam` and the `diva.india.context` response supplies Assam weather, screening, and source metadata. The stale values were coming from the retained `KER-WAY` assessment query and MapCommandWorkspace props, while large Nominatim boundaries can exceed GET URL limits. The fix is to use POST for `diva.india.context`, open location deep links in GIS mode, and bind the command panel/map footer/region labels to the live IndiaLocationContext when present.
Settled Assam verification (2026-08-26): After the POST context request completed, the map recentered on Assam and the command panel showed State: Assam, Assam weather, AQI, screening risk, and selected-location source text. One stale presentation remained: the left Region > District control fell back to the retained Wayanad assessment district for a state-level selection. State-level searches should show `State extent` rather than Wayanad.
Settled dashboard verification (2026-08-26): `/?location=assam` now retains the original dashboard shell while selected India panels show Assam, state-level address, live modelled weather, AQI, forecast, risk, population availability, infrastructure status, source disclosures, map context, and summary values. The retained Kerala assessment card is hidden/replaced in the selected-location dashboard state; its actions remain available only through an explicit `Open retained Kerala assessment actions` control. GIS mode uses the same context and no longer shows Wayanad values.
GIS pending-context finding (2026-08-26): Immediate screenshots of `?location=assam&workspace=map` and `?location=banjara-hills&workspace=map` showed the correct search badge and region controls but briefly retained the Wayanad assessment panel until `diva.india.context` finished loading. The command view must treat an explicit `location` URL as selected context even while the request is pending, showing selected-location placeholders rather than stale assessment values.
Final GIS verification (2026-08-26): `?location=assam&workspace=map` first presents `Resolving Assam, India context`, then settles on the Assam-centered map with `State extent`, Assam-specific risk/weather/AQI/forecast values, and no retained Wayanad panel. The selected-location dashboard path continues to expose the original dashboard shell and all selected-location decision panels.
Final locality verification (2026-08-26): `?location=banjara-hills&workspace=map` first shows `Resolving Banjara Hills, Hyderabad, Telangana, India context`, then settles with State Telangana, District Hyderabad, Locality: Banjara Hills, Hyderabad map framing, and locality-specific temperature, AQI, forecast, screening, and population availability values. No Wayanad assessment details are shown.
Invalid deep-link verification (2026-08-26): `?location=not-a-real-place&workspace=map` shows an explicit `Location fallback` notice, renders the default Andhra Pradesh context with `State extent`, uses default-context weather/screening values, and does not present the unknown query as a selected place or retained Wayanad assessment.
